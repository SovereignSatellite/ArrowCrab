# Routine installs must not mutate package.json or package-lock.json.
save=false
package-lock=true

# Any intentionally saved dependency must use an exact version.
save-exact=true

# Give newly published packages time to be inspected before resolution.
min-release-age=7
